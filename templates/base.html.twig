<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Bucket List{% endblock %}</title>
    <link rel="icon" href="{{ asset('images/favicon.png') }}" type="image/x-icon">

    {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
        <link rel="stylesheet" href="{{ asset('styles/style.css') }}">
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="d-flex flex-column min-vh-100">

<header class="bg-primary shadow-sm">
    <div class="container d-flex align-items-center justify-content-between py-3">
        <div class="d-flex align-items-center">
            <img src="{{ asset('images/logo.png') }}" alt="Logo" width="40" height="40" class="me-2">
            <h1 class="h5 text-white mb-0">Bucket List</h1>
        </div>
        <div class="d-flex gap-2">
            <a href="{{ path('app_main') }}" class="btn btn-outline-light">
                <i class="bi bi-house-door me-1"></i> Accueil
            </a>
            <a href="{{ path('wish_list') }}" class="btn btn-outline-light">
                <i class="bi bi-stars me-1"></i> Liste des souhaits
            </a>
            <a href="{{ path('app_about') }}" class="btn btn-outline-light">
                <i class="bi bi-info-circle me-1"></i> À propos
            </a>
        </div>
    </div>
</header>

<main class="flex-grow-1">
    {% block body %}{% endblock %}
</main>

<footer class="bg-light text-center py-4 mt-auto border-top">
    <div class="container">
        <p class="mb-1">© {{ "now"|date("Y") }} <strong>Bucket List</strong> — Application Symfony</p>
        <small class="text-muted">Réalisé avec ❤️ et Bootstrap 5</small>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoYz1D1zWc5I6p6n6Zl1Z9Zl5Zl1Z9Zl5Zl1Z9Zl5Zl1Z9Z" crossorigin="anonymous"></script>
</body>
</html>
